export default function LocaleLoading() {
  // Return null — Skeletons inside page components handle visual loading state
  // This prevents any top progress bar or spinner from blocking or flashing during navigation
  return null;
}